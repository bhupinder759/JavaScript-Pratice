//Check if object is empty

const obj = {};

console.log(Object.keys(obj).length === 0); // true, kyuki obj object me koi keys nahi hai, isliye Object.keys(obj) se empty array milega aur uski length 0 hogi, isliye output true hoga